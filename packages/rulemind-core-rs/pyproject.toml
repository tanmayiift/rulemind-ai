[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "rulemind-core-rs"
version = "0.1.0"
description = "RuleMind decision eval-core (Rust, via PyO3)"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
