apiVersion: v1
kind: Service
metadata:
  name: rulemind-web
spec:
  type: {{ .Values.service.web.type }}
  selector:
    app: rulemind-web
  ports:
    - port: {{ .Values.service.web.port }}
      targetPort: 3000
