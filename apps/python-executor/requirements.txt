fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
cryptography==45.0.4
sqlalchemy==2.0.38
alembic==1.14.1
bcrypt==4.2.1
PyJWT==2.10.1
httpx==0.28.1
APScheduler==3.10.4
PyYAML==6.0.2
prometheus-client==0.21.1
redis==5.2.1
psycopg2-binary==2.9.9

# ML model hosting — required to unpickle and run uploaded models (advertised
# feature + /models UI + policy "model" step). Without these the runtime cannot
# deserialize real models. Full stack (sklearn + boosted trees); adds ~400MB.
numpy==2.2.1
scipy==1.15.1
scikit-learn==1.6.1
joblib==1.4.2
xgboost==2.1.3
lightgbm==4.5.0

# Observability (optional at runtime; tracing activates when OTEL_EXPORTER_OTLP_ENDPOINT is set)
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
